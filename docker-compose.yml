services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - crawler
    env_file:
      - .env
networks:
  crawler:


volumes:
  rabbitmq_data:
